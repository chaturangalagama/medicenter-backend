package com.ilt.cms.pm.integration.mapper;

@Deprecated
public class DispatchMapper extends Mapper {

/*    public static Dispatch mapToCore(DispatchEntity dispatchEntity) {
        Dispatch dispatch = new Dispatch();
        if (dispatchEntity == null) return dispatch;
        dispatch.setDispatchedQty(dispatchEntity.getDispatchedQty());
        return dispatch;
    }

    public static DispatchEntity mapToEntity(Dispatch dispatch) {
        DispatchEntity dispatchEntity = new DispatchEntity();
        if (dispatch == null) return dispatchEntity;
        if (dispatch.getDispatchedQty() != null)
        dispatchEntity.setDispatchedQty(dispatch.getDispatchedQty());
        return dispatchEntity;
    }*/
}
